modLoader   = "javafml"
loaderVersion = "${loader_version_range}"
license     = "MIT"

[[mods]]
    modId       = "${mod_id}"
    version     = "${mod_version}"
    displayName = "PétasseGang Addons"
    description = '''${mod_description}'''

    # URL shown in the mods screen — update once you have a GitHub repo URL
    # displayURL = "https://github.com/PetasseGang/petassegang_addons"

    # Path to a 256x256 logo inside the JAR (optional)
    # logoFile = "petassegang_addons.png"

    authors  = "${mod_authors}"
    credits  = "La PétasseGang crew"

[[dependencies.${mod_id}]]
    modId        = "forge"
    mandatory    = true
    versionRange = "${loader_version_range}"
    ordering     = "NONE"
    side         = "BOTH"

[[dependencies.${mod_id}]]
    modId        = "minecraft"
    mandatory    = true
    versionRange = "${minecraft_version_range}"
    ordering     = "NONE"
    side         = "BOTH"
